{% extends 'base.html.twig' %}

{% block title %}FamilleArticle{% endblock %}

{% block body %}
    <h1>FamilleArticle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ famille_article.id }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ famille_article.marque }}</td>
            </tr>
            <tr>
                <th>Modele</th>
                <td>{{ famille_article.modele }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ famille_article.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_famille_article_index') }}">back to list</a>

    <a href="{{ path('app_famille_article_edit', {'id': famille_article.id}) }}">edit</a>

    {{ include('famille_article/_delete_form.html.twig') }}
{% endblock %}
