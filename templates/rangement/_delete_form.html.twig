<form method="post" action="{{ path('app_rangement_delete', {'id': rangement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rangement.id) }}">
    <button class="btn">Delete</button>
</form>
