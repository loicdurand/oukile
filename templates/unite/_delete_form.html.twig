<form method="post" action="{{ path('oukile_unite_delete', {'id': unite.id}) }}" onsubmit="return confirm('Êtes vous sûr de vouloir supprimer cette entrée?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unite.id) }}">
	<button class="fr-btn fr-btn--tertiary">Supprimer</button>
</form>
