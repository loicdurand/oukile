{% extends 'base.html.twig' %}

{% block title %}TypeRangement{% endblock %}

{% block body %}
    <h1>TypeRangement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_rangement.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_rangement.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_rangement_index') }}">back to list</a>

    <a href="{{ path('app_type_rangement_edit', {'id': type_rangement.id}) }}">edit</a>

    {{ include('type_rangement/_delete_form.html.twig') }}
{% endblock %}
